import HeroHeader from "@/components/HeroHeader";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <HeroHeader />
    </div>
  );
};

export default page;
